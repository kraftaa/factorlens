apiVersion: apps/v1
kind: Deployment
metadata:
  name: factorlens-mcp
  namespace: analytics
  labels:
    app: factorlens-mcp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: factorlens-mcp
  template:
    metadata:
      labels:
        app: factorlens-mcp
    spec:
      containers:
      - name: factorlens-mcp
        image: ghcr.io/kraftaa/factorlens-mcp:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8000
        env:
        - name: FACTORLENS_BIN
          value: /app/bin/factorlens
        envFrom:
        - configMapRef:
            name: factorlens-mcp-config
        - secretRef:
            name: factorlens-mcp-secrets
            optional: true
        volumeMounts:
        - name: data
          mountPath: /data
          readOnly: true
        - name: profiles
          mountPath: /profiles
          readOnly: true
        - name: artifacts
          mountPath: /artifacts
        readinessProbe:
          tcpSocket:
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: http
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "2"
            memory: "2Gi"
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: factorlens-data-pvc
      - name: profiles
        persistentVolumeClaim:
          claimName: factorlens-profiles-pvc
      - name: artifacts
        persistentVolumeClaim:
          claimName: factorlens-artifacts-pvc
