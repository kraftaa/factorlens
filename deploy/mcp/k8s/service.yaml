apiVersion: v1
kind: Service
metadata:
  name: factorlens-mcp
  namespace: analytics
  labels:
    app: factorlens-mcp
spec:
  selector:
    app: factorlens-mcp
  ports:
  - name: http
    port: 80
    targetPort: 8000
  type: ClusterIP
